# dependencies and build output (reproducible: npm install && npm run build)
node_modules/
contracts/artifacts/
contracts/cache/
contracts/types/
# keeper runtime cursor
keeper/state.json
# local env, never commit keys
.env
.env.*
# vercel link state
.vercel/

# keys made on this machine for the keeper or a deployer: never commit
keeper/*.key
contracts/*.key
keeper/odds-state.json
keeper/keeper.log
