{
  "name": "zolt-odds-site",
  "private": true,
  "description": "The deploy folder: the static page plus three small functions (a read-only RPC relay, the markets JSON, and the link-preview cards).",
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2",
    "satori": "^0.33.5"
  }
}
